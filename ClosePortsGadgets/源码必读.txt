关闭端口小工具
源码描述：
一、源码特点
      解决某些情况端口被不是自己的程序所占用，无法关闭。使用方法，填写端口号后获取占用端口的程序的PID，右键后程序会根据PID找到找到那个程序（默认会自动选中）此时点击窗口上面的关闭端口就会关掉那个程序
二、菜单功能
      1、在输入框中输入端口好，点击获取开启端口的程序，即可在下方显示端口信息
      2、在窗体中选择相应端口，然后点击关闭端口，即可关闭指定端口程序
三、注意事项
      1、开发环境为Visual Studio 2010，使用.net3.5开发。
作者：soeazy

